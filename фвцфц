#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char str[255][255];
    int n;
    scanf("%d",&n);
    for(int i=0;i<=n;i++){
        fgets(str[i],255,stdin);
    }

    for(int i=0;i<=n;i++){   
        if(strcmp(str[i],str[n])){
            printf("%s", str[i]);
        }
    }
    return 0;
}
